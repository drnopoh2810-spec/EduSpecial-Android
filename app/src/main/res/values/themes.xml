<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main app theme -->
    <style name="Theme.EduSpecial" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Splash screen theme — uses core-splashscreen library -->
    <style name="Theme.EduSpecial.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.EduSpecial</item>
    </style>
</resources>
